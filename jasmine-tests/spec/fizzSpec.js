describe("my FizzBuzz game", function() {

    beforeEach(function) {
        number = new fizzBuzz();
    }

    describe("Check number", function() [
        it("should be devisible by 3 and 5(14)", function() {
            var number = (number % 3 === 0 && number % 5 === 0)
            expect(number).toBe("FizzBuzz");
        })
    ])

});